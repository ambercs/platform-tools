---
platform: linux

params:
  SKIP_SSL_VALIDATION: false
  OPSMAN_URL:
  OPSMAN_USERNAME:
  OPSMAN_PASSWORD:

run:
  path: bash
  args:
  - -c
  - |
    set -eu

    eval “$(om bosh-env)”

    bosh clean-up --all --keep-orphaned-disks
